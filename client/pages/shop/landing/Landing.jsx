import React, { PropTypes, Component } from 'react';

class LandingPage extends Component {

    static propTypes = {

    };

    static defaultProps = {

    };

    render() {
        return (<div onClick={this.onClick}>Landing page</div>);
    }
}

export default LandingPage;
